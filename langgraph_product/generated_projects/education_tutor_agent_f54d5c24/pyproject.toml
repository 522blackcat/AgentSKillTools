[project]
name = "education-tutor-agent"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi",
  "uvicorn",
  "sqlalchemy",
  "psycopg[binary]",
  "langgraph",
  "openai",
  "pypdf",
  "python-docx",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
